import 'package:flutter/material.dart';

InputDecoration styleLabelText(String labelText) => InputDecoration(
      labelText: labelText,
      labelStyle: TextStyle(
        color: Colors.black,
        fontSize: 14
    )
);

TextStyle styleFieldText() => TextStyle(
    color: Colors.black,
    fontSize: 18
);
